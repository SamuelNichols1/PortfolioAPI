"""
Config Package Initializer

This file makes the 'config' directory a Python package.
An empty __init__.py file tells Python that this directory contains Python modules.

In Django projects, the config package contains:
- settings.py (project settings)
- urls.py (root URL configuration)
- wsgi.py (WSGI server interface)
- asgi.py (ASGI server interface)

This file is typically left empty but can be used to run initialization code
when the package is imported.
"""
